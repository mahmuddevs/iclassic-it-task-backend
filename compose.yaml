services:
  iclassic-backend:
    build: .
    image: iclassic-backend
    ports:
      - 3000:3000
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: bun start
